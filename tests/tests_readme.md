Create an env from `tests/test_env.yml`

Run all the tests in a pytest file; with no verbosity:
```commandline
pytest tests/test_llm.py
```

With verbosity:
```commandline
pytest tests/test_llm.py -v
```
